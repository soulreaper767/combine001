<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_tax_adjustment_list" model="ir.ui.view">
        <field name="name">combine001.tax.adjustment.list</field>
        <field name="model">combine001.tax.adjustment</field>
        <field name="arch" type="xml">
            <list string="Manual Tax Ledger Adjustments">
                <field name="name"/>
                <field name="tax_id"/>
                <field name="partner_id"/>
                <field name="original_value"/>
                <field name="revised_value"/>
                <field name="requested_by"/>
                <field name="approved_by"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'pending'"
                       decoration-success="state == 'approved'"
                       decoration-danger="state == 'rejected'"/>
            </list>
        </field>
    </record>

    <record id="view_tax_adjustment_form" model="ir.ui.view">
        <field name="name">combine001.tax.adjustment.form</field>
        <field name="model">combine001.tax.adjustment</field>
        <field name="arch" type="xml">
            <form string="Manual Tax Ledger Adjustment">
                <header>
                    <button name="action_submit" string="Submit for Approval" type="object"
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_approve" string="Approve" type="object"
                            class="oe_highlight" invisible="state != 'pending'"
                            groups="account.group_account_manager"/>
                    <button name="action_reject" string="Reject" type="object"
                            invisible="state != 'pending'"
                            groups="account.group_account_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,pending,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="tax_id"/>
                            <field name="partner_id"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="original_value"/>
                            <field name="revised_value"/>
                        </group>
                        <group>
                            <field name="requested_by"/>
                            <field name="approved_by"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <group>
                        <field name="reason" nolabel="1" placeholder="Reason for this manual adjustment..."/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_tax_adjustment" model="ir.actions.act_window">
        <field name="name">Manual Tax Ledger Adjustments</field>
        <field name="res_model">combine001.tax.adjustment</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Request a controlled, auditable manual adjustment to the Tax Ledger.
            </p>
        </field>
    </record>
</odoo>
